# Director Digest — 2026-09-18

Снапшот за последние 24 часа (2026-09-17 06:13 UTC → 2026-09-18 06:13 UTC).
Файл перезаписывается при каждом запуске.

## Коммиты за 24ч

3 коммита в `main` за окно:

- `2df81fa` 06:35 UTC — `qa: video lint` (github-actions[bot])
- `de0d534` 06:41 UTC — `chore: health check blocked` (23-й раз подряд, Claude)
- `0bca1a3` 12:34 UTC — `qa: video lint` (github-actions[bot])

**После 12:34:59 UTC 17 сентября в `main` не было ни одного коммита** —
это ~17.5 часов без единой записи на момент генерации этого дайджеста.
Ни одного человеческого и ни одного product-коммита в окне нет.

## ВАЖНОЕ НОВОЕ: Video QA Gate реально сломался (не просто «не отчитался»)

Проверил напрямую через GitHub Actions API (`Video QA Gate`,
`.github/workflows/video-qa.yml`, cron `17 */6 * * *`):

- Запуск **#32** (2026-09-17 18:32 UTC) — **failure**.
- Запуск **#33** (2026-09-18 00:34 UTC) — **failure**.
- Оба упали на одном и том же шаге «Run QA gate against pending reels»
  с `exit code 22` — это код `curl -sf` при получении HTTP-ошибки
  (4xx/5xx) от `$WEBAPP_URL/api/queue?status=pending&kind=reel`, **не**
  код сетевой недоступности. GitHub Actions runner не идёт через
  заблокированный egress-прокси облачной песочницы — то есть это не та
  же проблема, что в HEALTH-LOG.md, а отдельная, реальная ошибка на
  стороне продакшн-API (либо `ADMIN_KEY` протух/сменился, либо сам
  вебапп отдаёт ошибку на этот эндпоint).
- Из-за `set -e` скрипт падает до записи в `VIDEO-QA-LOG.md` и до
  коммита — именно поэтому в логе и в git не появилось новых записей
  после 12:34:58 UTC. Это не «гейт не запускался», а «гейт запускается
  по расписанию и падает».
- **Практическое следствие:** любые ролики, добавленные в очередь после
  18:32 UTC 17 сентября, сейчас не проверяются и не аппрувятся —
  зависают в `pending` до починки.
- Следующий плановый запуск — ~06:17 UTC 18 сентября; на момент
  генерации дайджеста (06:13 UTC) ещё не наступил.

## HEALTH-LOG.md — статус: НОВЫХ ЗАПИСЕЙ НЕТ (в отличие от вчера)

Последняя запись — 06:41 UTC 17 сентября («ПРОВЕРКА НЕ ВЫПОЛНЕНА»,
23-й раз подряд, та же причина: `403 connect_rejected` egress-прокси на
домены Cloudflare, `github.com` при этом доступен). До этой записи
рутина стабильно отчитывалась каждые ~6 часов 5+ суток подряд (23 раза
без единого успеха). За прошедшие ~23.5 часа новых записей не появилось
— пропущены 3 ожидаемых цикла (~12:4x и ~18:4x 17-го, ~00:4x 18-го).
Не могу подтвердить отсюда, перестала ли запускаться именно эта
scheduled-сессия или причина иная — фиксирую только сам факт разрыва в
логе, которого не было ни разу за предыдущие 5+ суток.

## VIDEO-QA-LOG.md — статус: СЛОМАН (детали выше)

Последняя валидная запись — 12:34:58 UTC 17 сентября (`pending reels:
0`). Дальше — 2 подряд упавших прогона в GitHub Actions, ни один не
дошёл до записи в лог. Файл сейчас отстаёт от реальности примерно на
18 часов.

## DEPENDENCY-AUDIT.md — статус: БЕЗ ИЗМЕНЕНИЙ (недельная задача, не в окне)

Последняя запись — 2026-09-16 (patch-апдейт `remotion`/`@remotion/cli`).
Новых записей за 24ч нет — ожидаемо, следующий прогон ~2026-09-23. Не
признак сбоя.

## Признаки застоя проекта

По формальному счётчику коммитов порог «>2 суток без коммитов от
облачных агентов» пока не пройден (последний коммит — 17.5ч назад). Но
впервые за наблюдаемый период **обе** ранее надёжные автоматизации
(video-QA и health-check) замолчали в одном и том же 24-часовом окне —
и для одной из них (video-QA) теперь есть прямое подтверждение реальной
поломки на стороне продакшн-API, а не просто «нечего проверять».
Product-разработка стоит уже 4-й день подряд: последний
человеческий/product-коммит — `5e31d3e` (2026-09-14, 16:05 MSK /
~13:05 UTC), это ~3.7 суток назад.

## Итог для владельца

1. **Срочно и новое:** Video-QA gate реально ломается на проде (HTTP-
   ошибка от `$WEBAPP_URL/api/queue`, подтверждено 2 упавшими прогонами
   GitHub Actions подряд) — проверить валидность `ADMIN_KEY` в secrets
   репозитория и статус самого вебаппа напрямую; пока не починено,
   новые ролики в очереди не проверяются и не публикуются.
2. Health-check (облачная песочница) — блокировка Cloudflare не снята,
   плюс сама рутина не отчиталась за последние ~23.5ч (разрыв, которого
   раньше не было) — стоит проверить, срабатывает ли эта
   scheduled-задача вообще.
3. Dependency-audit — без изменений, недельная задача, всё по графику.
4. Product-разработка стоит 4-й день подряд (с 2026-09-14) — ни одного
   содержательного коммита ни от человека, ни от агентов.
