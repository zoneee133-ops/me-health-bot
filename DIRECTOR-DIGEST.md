# Director Digest — 2026-09-20

Снапшот за последние 24 часа (2026-09-19 06:11 UTC → 2026-09-20 06:12 UTC).
Файл перезаписывается при каждом запуске.

## Коммиты за 24ч: 0

`git log --since="24 hours ago"` показывает только один коммит —
`e0ca452` (вчерашний дайджест, 06:11:45 UTC 19 сентября), который попадает
в окно ровно по границе (создан за ~28 секунд до начала 24-часового окна
этого запуска). За прошедшие сутки не появилось ни одного нового коммита
ни от одного облачного агента, ни от человека. Второе подряд полностью
пустое 24-часовое окно.

## Health-check по-прежнему ВЫКЛЮЧЕН — без изменений 3-й день

Проверил реестр scheduled-задач напрямую: routine **«ME — Health Check &
Self-Heal»** (`trig_011EskLWP2zEXC3dmkKgSNk7`, cron `41 */6 * * *`)
остаётся в состоянии **`enabled: false`**, выключена **2026-09-17
07:43:59 UTC** — с тех пор ничего не изменилось. С момента отключения
прошло уже **~70.5 часов (почти 3 суток)**, и никто её не включил обратно.
HEALTH-LOG.md по-прежнему заканчивается записью 06:41 UTC 17 сентября
(23-й провал подряд из-за блокировки egress на Cloudflare). Прод сейчас
**вообще никем не мониторится**.

## Video QA Gate (GitHub Actions) — сломан уже 10 запусков подряд, ~60 часов

`Video QA Gate` (`.github/workflows/video-qa.yml`, cron `17 */6 * * *`)
через GitHub Actions API:

- Запуски **#32–#41** (2026-09-17 18:32 UTC → **2026-09-20 04:56 UTC**,
  последний ~1.3ч назад на момент проверки) — **10 провалов подряд без
  единого успеха**. Вчера было зафиксировано 6 провалов — за прошедшие
  сутки проблему никто не тронул, счётчик просто вырос ещё на 4.
- Причина не изменилась: ошибка на шаге получения очереди
  (`curl -sf $WEBAPP_URL/api/queue?status=pending&kind=reel`, exit 22 —
  HTTP-ошибка от продакшн-API, не сетевая блокировка раннера).
- **Практическое следствие:** ролики, добавленные в очередь после 18:32
  UTC 17 сентября, по-прежнему не проходят QA и не публикуются — уже
  почти 3 дня.

## ME — Video QA Lint (отдельная routine) — отчитывается «успешно», но не оставляет следов уже 3-й день подряд

Это отдельный механизм (не GitHub Actions), cron `49 */12 * * *`. По
реестру её последние прогоны:

- 2026-09-20 00:49 UTC → `SUCCEEDED` (finished 00:52 UTC)
- Прошлые прогоны за 19–20 сентября — тоже все `SUCCEEDED`

Но ни один из них не породил ни коммита `qa: video lint`, ни новой строки
в VIDEO-QA-LOG.md — последняя запись там всё ещё от 12:34:58 UTC 17
сентября (разрыв уже ~65.5ч). Это то же расхождение, что было замечено во
вчерашнем дайджесте, но теперь оно устойчиво держится **второй день
подряд**: сессия завершается без ошибки, но результат нигде не
фиксируется — либо очередь стабильно пуста и агент интерпретирует шаг 6
инструкции («в любом случае допиши…») слишком буквально только при
непустой очереди, либо запрос к API тихо проваливается до записи в лог.
Не могу проверить транскрипт этих сессий отсюда, чтобы отличить один
вариант от другого — фиксирую расхождение как факт.

## DEPENDENCY-AUDIT.md — без изменений, по графику

Последняя запись — 2026-09-16. Routine «ME — Dependency Security Audit»
следующий раз должна сработать 2026-09-23 (еженедельно). Не признак сбоя.

## Остальные weekly-routines — по графику, не просрочены

Backup Snapshot, Weekly Director Report, Weekly Competitor Research,
Weekly Reel Performance Analysis, Creative Format Research, KPI Threshold
Tracker — у всех последний успешный прогон 2026-09-14, следующий —
2026-09-21 (понедельник). Ждать нечего, поводов для тревоги нет.

## Признаки застоя проекта — подтверждены, ситуация не улучшилась

- **Коммитов за 24ч: 0** содержательных — второй день подряд.
- **Продуктовых/человеческих коммитов нет уже 6-й день подряд** (с
  `5e31d3e`, 2026-09-14 13:05 UTC).
- Обе технические автоматизации, отвечающие за прод (health-check) и
  качество контента (video-QA gate + video-QA lint), сейчас одновременно
  либо выключены, либо ломаются, либо отчитываются без результата — и
  так уже 3-й день без единого действия по устранению с чьей-либо
  стороны.

## Итог для владельца

1. **Прод не мониторится 3-й день подряд** — health-check routine
   выключена с 2026-09-17 07:43 UTC, никто не включил обратно. Если
   решение осознанное — ок, но тогда стоит явно знать, что алертов о
   падении прода сейчас не будет ни от кого.
2. **Video-QA Gate сломан ~60 часов** (10 провалов подряд, exit 22 на
   запросе к `$WEBAPP_URL/api/queue`) — со вчера проблему никто не тронул.
   Нужно проверить `ADMIN_KEY` в secrets репозитория и прямой ответ
   `$WEBAPP_URL/api/queue`.
3. **ME — Video QA Lint** второй день подряд отчитывается «успешно», но
   не оставляет ни коммита, ни записи в логе — стоит заглянуть в
   транскрипт одной из этих сессий, если нужен рабочий резервный канал
   для QA.
4. Dependency-audit и weekly-routines — без изменений, по графику,
   поводов для тревоги нет.
5. **Продуктовая разработка стоит уже 6-й день подряд** (с 2026-09-14).
