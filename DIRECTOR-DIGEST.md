# Director Digest — 2026-09-21

Снапшот за последние 24 часа (2026-09-20 06:20 UTC → 2026-09-21 06:20 UTC).
Файл перезаписывается при каждом запуске.

## Коммиты за 24ч: 1, чисто технический

`git log --since="24 hours ago"` показывает ровно один коммит —
`58fd67e` (2026-09-21 04:06 UTC), «chore: weekly backup snapshot —
2026-09-21» — плановый еженедельный бэкап, не содержательное изменение.
Ни продуктового, ни человеческого коммита за сутки не было. Вчерашний
дайджест (`23323ab`, 06:12:46 UTC 20 сентября) в это окно не попадает —
создан за ~7 минут до его начала.

## Health-check по-прежнему ВЫКЛЮЧЕН — 5-й день подряд

Проверил реестр scheduled-задач напрямую: routine **«ME — Health Check &
Self-Heal»** (`trig_011EskLWP2zEXC3dmkKgSNk7`, cron `41 */6 * * *`)
остаётся **`enabled: false`**, выключена **2026-09-17 07:43:59 UTC** — с
тех пор ничего не менялось (`updated_at` то же самое). С момента
отключения прошло уже **~94.6 часа (почти 4 суток)**. HEALTH-LOG.md
по-прежнему заканчивается записью 06:41 UTC 17 сентября (23-й провал
подряд из-за блокировки egress на Cloudflare). Прод **всё ещё вообще
никем не мониторится**, и это уже 5-й день, когда дайджест это
фиксирует, а ситуация не меняется.

## Video QA Gate (GitHub Actions) — сломан уже 14 запусков подряд, ~90 часов

`Video QA Gate` (`.github/workflows/video-qa.yml`, cron `17 */6 * * *`)
через GitHub Actions API:

- Последний успешный запуск — **#31**, 2026-09-17 12:34 UTC. С тех пор
  запуски **#32–#45** (по 2026-09-21 04:59 UTC включительно) — **14
  провалов подряд без единого успеха**, прирост на 4 провала за
  прошедшие сутки.
- Причина не менялась ни разу за это время (по логам предыдущих
  проверок): ошибка на шаге получения очереди
  (`curl -sf $WEBAPP_URL/api/queue?status=pending&kind=reel`, exit 22 —
  HTTP-ошибка от продакшн-API, не сетевая блокировка раннера).
- **Практическое следствие не изменилось:** ролики, добавленные в очередь
  после 18:32 UTC 17 сентября, всё ещё не проходят QA и не публикуются —
  почти 4 дня подряд, и за сутки никто это не тронул.

## ME — Video QA Lint (отдельная routine) — отчитывается «успешно», но не оставляет следов уже 4-й день подряд

Отдельный механизм (не GitHub Actions), cron `49 */12 * * *`. Последний
запуск по реестру: **2026-09-21 00:49:37 UTC → `SUCCEEDED`** (finished
00:51:23 UTC) — то есть уже сегодня, внутри проверяемого 24-часового
окна. Но этот запуск, как и все предыдущие с 17 сентября, не оставил ни
коммита `qa: video lint`, ни новой строки в VIDEO-QA-LOG.md — последняя
запись там всё ещё от 12:34:58 UTC 17 сентября (разрыв уже ~90 часов).
Расхождение то же самое, что фиксировалось три дня подряд: сессия
завершается без ошибки в реестре, но результат нигде не сохраняется.
Причину отсюда подтвердить не могу (нет доступа к транскрипту сессии).

## DEPENDENCY-AUDIT.md — без изменений, по графику

Последняя запись — 2026-09-16. Routine «ME — Dependency Security Audit»
следующий раз должна сработать 2026-09-23 (еженедельно, `0 5 * * 3`).
Не признак сбоя.

## Остальные weekly-routines — сработали сегодня по графику

KPI Threshold Tracker, Weekly Director Report, Weekly Competitor
Research, Weekly Reel Performance Analysis, Creative Format Research —
все запланированы на сегодня, понедельник 2026-09-21 (между 07:02 и
11:02 UTC), т.е. ещё не должны были сработать на момент этой проверки
(06:20 UTC). Их последний фактический прогон — 2026-09-14, следующий
ожидается сегодня. Weekly Backup Snapshot уже отработал сегодня в 04:06
UTC — успешно.

## Признаки застоя проекта — подтверждены, ситуация не улучшилась уже несколько дней

- **Коммитов за 24ч: 1**, и тот чисто технический (бэкап) — содержательных
  коммитов не было.
- **Продуктовых/человеческих коммитов нет уже 7-й день подряд** (с
  `5e31d3e`, 2026-09-14 13:05 UTC).
- Обе технические автоматизации, отвечающие за прод (health-check) и
  качество контента (video-QA gate + video-QA lint), одновременно либо
  выключены, либо ломаются, либо отчитываются без результата — и так уже
  4-й день подряд без единого действия по устранению с чьей-либо
  стороны.

## Итог для владельца

1. **Прод не мониторится уже 4-й день подряд** — health-check routine
   выключена с 2026-09-17 07:43 UTC, никто её не включил обратно. Если
   решение осознанное — ок, но тогда стоит явно знать, что алертов о
   падении прода сейчас ни от кого не будет.
2. **Video-QA Gate сломан ~90 часов подряд** (14 провалов, exit 22 на
   запросе к `$WEBAPP_URL/api/queue`) — за сутки проблему никто не
   тронул. Нужно проверить `ADMIN_KEY` в secrets репозитория и прямой
   ответ `$WEBAPP_URL/api/queue`.
3. **ME — Video QA Lint** уже 4-й день подряд отчитывается «успешно», но
   не оставляет ни коммита, ни записи в логе — стоит заглянуть в
   транскрипт одной из этих сессий, если нужен рабочий резервный канал
   для QA.
4. Dependency-audit — без изменений, по графику, поводов для тревоги нет.
5. **Продуктовая разработка стоит уже 7-й день подряд** (с 2026-09-14).
